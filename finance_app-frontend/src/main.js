import { createApp } from 'vue'
import App from './App.vue'
import router from './views/index' // Import the router configuration


createApp(App).use(router).mount('#app')
//  createApp(App).mount('#app')
